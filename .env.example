# Your chat backend (required). The frontend proxies /api/chat to this URL.
BACKEND_URL="http://localhost:8787"

# Optional: path on your backend that accepts AI SDK chat requests.
# BACKEND_CHAT_PATH="/api/chat"
